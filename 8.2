#include "stdafx.h"
#include <fstream>
#include <iostream>
#include <string>
using namespace std;

int main()
{
	string s;
	ifstream file("file1.txt");
	if (file.is_open()){
	for (file >> s; !file.eof(); file >> s) 
		cout << s << endl;
	}
	else
	{
		return -1;
	}
	system("pause");
	return 0;
}
